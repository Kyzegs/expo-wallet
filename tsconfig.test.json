// Type-checks the tests and the Jest mocks, which the build leaves out.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "."
  },
  "include": ["./src", "./mocks"],
  "exclude": []
}
